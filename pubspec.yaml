name: app_date_picker_bottom_sheet
description: A customizable Flutter date picker bottom sheet with smooth animations, custom themes, and an easy-to-use API.
version: 0.1.0


environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0